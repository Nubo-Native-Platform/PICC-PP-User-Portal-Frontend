export interface FeatureElement {
  assigned: boolean;
  elementDesc: string;
  elementId: string;
  elementName: string;
  elementPage: string;
  elementType: string;
  feaSeq: string;
  featureId: string;
}
